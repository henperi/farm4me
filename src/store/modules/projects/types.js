const SET_PROJECTS = 'project/SET_PROJECTS';
const UPDATE_ONE_PROJECT = 'project/UPDATE_ONE_PROJECT';
const ADD_PROJECT = 'project/ADD_PROJECT';

export const types = {
  SET_PROJECTS,
  UPDATE_ONE_PROJECT,
  ADD_PROJECT,
};
